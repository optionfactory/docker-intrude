[package]
name = "docker-intrude"
version = "1.0.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["sched", "process", "user", "signal", "mount"] }
caps = "0.5.6"
libc = "0.2"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
